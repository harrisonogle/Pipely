\* TLC configuration for Publication.tla — correct model (ordered splice).
\* Expected: all invariants and temporal properties hold.
\*
\* Splice-ordering experiment: use Publication_Reversed.cfg to flip the
\* splice's first two release-stores and watch ChainConsistent fail.

SPECIFICATION Spec

CHECK_DEADLOCK FALSE

CONSTANTS
    MaxSegments           = 3
    MaxChainLen           = 2
    ENABLE_ORDERED_SPLICE = TRUE

INVARIANTS
    TypeOK
    ChainConsistent
    NoRetiredAsPrevTail
    BWPNotAheadOfTail
    ReaderHeadNotRetired
    ExaminedNotAhead

PROPERTIES
    EventualWriterHalt
    EventualReaderHalt
