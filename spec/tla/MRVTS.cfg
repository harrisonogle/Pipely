\* Default MRVTS configuration: correct axiom semantics, all invariants hold.
\* Run as:  tlc -config MRVTS.cfg MRVTSStandalone.tla
SPECIFICATION Spec

CONSTANTS
    Results       = {"R0", "R1"}
    Exceptions    = {"E0"}
    Callbacks     = {"cb0"}
    MaxResets     = 2
    MaxCompletes  = 3
    EnforceResetDiscipline = TRUE

INVARIANTS
    TypeOK
    VersionBounded
    ResetDiscipline
    NoOrphanDispatch
